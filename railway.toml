[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @colonus/api build"

[deploy]
startCommand = "pnpm --filter @colonus/api start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
